# 別ハーネス・ウォークスルー（P2 B10-4）

P1 の [walkthrough-checklist.md](./walkthrough-checklist.md) を **別ハーネス**（初回と違うもの）で再実施する。  
例: 初回が Claude なら Cursor または Codex。

## 前提

- B10-3 と同じクリーン手順（`npm run setup` → `dev:all`）
- 貼る文は `/setup` に出るものをそのまま使う（手直し禁止）

## 手順（介入なし）

1. [ ] 初回と **違う** LLM に MCP を登録
2. [ ] サンプルしれん提出（Web）
3. [ ] 貼る文だけで `list_pending_gates` が自発呼び出し
4. [ ] `morning_briefing` 冒頭に「今日のしれん」「昨日の判定」が見える
5. [ ] `answer_gate` で1問提出（合否断定を LLM にさせない）
6. [ ] （任意）`request_gate` で差分から1しれん

## 詰まり記録

| # | 手順 | 何が起きた | 期待 | 深刻度 | 対応 |
|---|---|---|---|---|---|
| 1 |  |  |  |  |  |

## 完了判定

- [ ] 無介入で初回ループ完走（別ハーネス）
- [ ] `docs/phase-progress.md` の B10-4 を `done` に更新

実施日: ____ / ハーネス: ____ / 所要分: ____
